#!<%= ENV["SHELL"] || "/bin/bash" %>

<%- if tmux_has_session? name -%>
  cd <%= root || "." %>

  <%= tmux_kill_session_command %>
  
  # Run on_project_stop command
  <%= hook_on_project_stop %>

<%- end -%>